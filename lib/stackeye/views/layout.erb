<!doctype html>
<html lang="en">
  <head>
    <!-- Metatags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="shortcut icon" href="<%= base_url %>/images/favicon.ico" type="image/x-icon">
    <% if cookies.key?(:refresh) %>
      <meta http-equiv="refresh" content="60">
    <% end %>
    <title>Stackeye</title>

    <!-- Stylesheets -->
    <link rel="stylesheet" href="<%= base_url %>/stylesheets/feather.css">
    <link rel="stylesheet" href="<%= base_url %>/stylesheets/bootstrap.css">
    <link rel="stylesheet" href="<%= base_url %>/stylesheets/application.css">
  </head>
  <body>
    <!-- Components -->
    <%= erb(:"shared/_navbar") %>
    <div class="main-content">
      <div class="container-fluid">
        <%= yield %>
      </div>
    </div>

    <!-- Javascripts -->
    <script src="<%= base_url %>/javascripts/jquery.js"></script>
    <script src="<%= base_url %>/javascripts/moment.js"></script>
    <script src="<%= base_url %>/javascripts/popper.js"></script>
    <script src="<%= base_url %>/javascripts/bootstrap.js"></script>
    <script src="<%= base_url %>/javascripts/chart.js"></script>
    <script src="<%= base_url %>/javascripts/application.js"></script>
  </body>
</html>
